{
  "name": "pr-ui-compare",
  "displayName": "PR UI Compare",
  "publisher": "bouzidanas",
  "description": "Create synchronized Before and After UI comparisons for pull requests with Playwright.",
  "version": "0.0.17",
  "author": "Anas Bouzid",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/bouzidanas/playwright-comparison-gif-maker.git"
  },
  "homepage": "https://github.com/bouzidanas/playwright-comparison-gif-maker#readme",
  "bugs": {
    "url": "https://github.com/bouzidanas/playwright-comparison-gif-maker/issues"
  },
  "engines": {
    "vscode": "^1.125.0"
  },
  "categories": [
    "Testing",
    "Visualization"
  ],
  "keywords": [
    "playwright",
    "pull-request",
    "gif",
    "visual-comparison",
    "ui-testing",
    "copilot",
    "agent-tools"
  ],
  "extensionKind": [
    "workspace"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": false,
      "description": "PR UI Compare runs project install and development server commands."
    },
    "virtualWorkspaces": false
  },
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "pr-ui-compare.createComparison",
        "title": "Create PR UI Comparison",
        "category": "PR UI Compare",
        "icon": "$(preview)"
      },
      {
        "command": "pr-ui-compare.installBrowser",
        "title": "Install Managed Chromium",
        "category": "PR UI Compare",
        "icon": "$(cloud-download)"
      }
    ],
    "languageModelTools": [
      {
        "name": "pr-ui-compare_createComparison",
        "displayName": "Create PR UI Comparison",
        "modelDescription": "Creates a labeled Before and After comparison without creating or modifying files in the user's repository. Animation is the default and must be used whenever anything happens or changes, including any event, interaction, hover, transition, loading sequence, resize, zoom, scroll, opening, closing, or state mutation. Set outputMode to image only when the user explicitly requests static images or the subject is truly motionless and requires no actions at all. Image mode accepts an empty action list only and captures the settled initial route as PNG files. When uncertain, always choose animation. The tool resolves a baseline Git ref into a detached worktree, starts both applications sequentially, stores artifacts outside the repository, and opens a preview. Never write a JavaScript, TypeScript, Playwright, or shell helper script. Border color, color scheme, layout, focus crop, and label corners are configurable. The start and optional install commands execute project code and require user confirmation.",
        "userDescription": "Create synchronized GIF UI comparisons by default, or static PNGs only for explicit, completely motionless cases.",
        "canBeReferencedInPrompt": true,
        "toolReferenceName": "createPrUiComparison",
        "icon": "$(record)",
        "tags": [
          "testing",
          "playwright",
          "pull-request",
          "visualization"
        ],
        "inputSchema": {
          "type": "object",
          "properties": {
            "outputMode": {
              "type": "string",
              "enum": [
                "animation",
                "image"
              ],
              "description": "Output type. animation is the default and is required whenever anything happens or changes. image is only for explicit or truly motionless, action-free initial-route PNG comparisons."
            },
            "frameRate": {
              "type": "integer",
              "minimum": 5,
              "maximum": 30,
              "description": "Animated GIF frame rate. Defaults to 24. Use 30 for fast visual events; lower values reduce file size for slow motion. Not available in image mode."
            },
            "colorScheme": {
              "type": "string",
              "enum": [
                "light",
                "dark",
                "system"
              ],
              "description": "Preferred browser color scheme for both versions. system disables emulation and uses host browser behavior. Defaults to system."
            },
            "beforeColorScheme": {
              "type": "string",
              "enum": [
                "light",
                "dark",
                "system"
              ],
              "description": "Optional color scheme override for the Before browser."
            },
            "afterColorScheme": {
              "type": "string",
              "enum": [
                "light",
                "dark",
                "system"
              ],
              "description": "Optional color scheme override for the After browser."
            },
            "baseRef": {
              "type": "string",
              "description": "Git branch, tag, or commit for the Before application, normally upstream/main or the PR target branch."
            },
            "startCommand": {
              "type": "string",
              "description": "Shell command that starts either application. Use {port} where a dynamically allocated port should be inserted."
            },
            "readyUrl": {
              "type": "string",
              "description": "HTTP URL polled until the application is ready. Use {port} for the allocated port."
            },
            "installCommand": {
              "type": "string",
              "description": "Optional dependency installation command run only in the temporary Before worktree."
            },
            "route": {
              "type": "string",
              "description": "Route opened before replay, such as /settings."
            },
            "beforeLabel": {
              "type": "string",
              "description": "Optional short label shown over the Before recording."
            },
            "afterLabel": {
              "type": "string",
              "description": "Optional short label shown over the After recording."
            },
            "beforeLabelAlignment": {
              "type": "string",
              "enum": [
                "top-left",
                "top-right",
                "bottom-left",
                "bottom-right"
              ],
              "description": "Corner for the Before label. Defaults to top-left."
            },
            "afterLabelAlignment": {
              "type": "string",
              "enum": [
                "top-left",
                "top-right",
                "bottom-left",
                "bottom-right"
              ],
              "description": "Corner for the After label. Defaults to top-right."
            },
            "borderColor": {
              "type": "string",
              "pattern": "^#[0-9a-fA-F]{6}$",
              "description": "Six-digit hex color for pane frames, the divider, and unused resize canvas. Defaults to GitHub dark border #30363d."
            },
            "focusLocator": {
              "type": "string",
              "description": "Optional Playwright locator for the UI region to crop around throughout the recording, such as a menu bar or panel."
            },
            "focusPadding": {
              "type": "number",
              "minimum": 0,
              "maximum": 256,
              "description": "Padding in pixels around the focused UI region. Defaults to 16."
            },
            "layout": {
              "type": "string",
              "enum": [
                "auto",
                "horizontal"
              ],
              "description": "Comparison arrangement. Keep auto unless side-by-side must be forced. Top-and-bottom output is selected only when the focused region is strictly greater than 3:1, and cannot be forced for narrower content."
            },
            "viewport": {
              "type": "object",
              "properties": {
                "width": {
                  "type": "number",
                  "minimum": 320,
                  "maximum": 3840
                },
                "height": {
                  "type": "number",
                  "minimum": 240,
                  "maximum": 2160
                }
              },
              "required": [
                "width",
                "height"
              ]
            },
            "scenario": {
              "type": "object",
              "properties": {
                "name": {
                  "type": "string",
                  "description": "Short description of the behavior being demonstrated."
                },
                "actions": {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "properties": {
                      "type": {
                        "type": "string",
                        "enum": [
                          "goto",
                          "click",
                          "hover",
                          "fill",
                          "press",
                          "scroll",
                          "resize",
                          "zoom",
                          "waitFor",
                          "hold"
                        ]
                      },
                      "locator": {
                        "type": "string",
                        "description": "Playwright locator string, preferably role=, text=, label=, or data-testid=."
                      },
                      "path": {
                        "type": "string"
                      },
                      "value": {
                        "type": "string"
                      },
                      "key": {
                        "type": "string"
                      },
                      "deltaX": {
                        "type": "number"
                      },
                      "deltaY": {
                        "type": "number"
                      },
                      "width": {
                        "type": "number",
                        "minimum": 320,
                        "maximum": 3840,
                        "description": "Target viewport width for a resize action."
                      },
                      "height": {
                        "type": "number",
                        "minimum": 240,
                        "maximum": 2160,
                        "description": "Target viewport height for a resize action."
                      },
                      "resizeMode": {
                        "type": "string",
                        "enum": [
                          "keep-left-edge-fixed",
                          "keep-right-edge-fixed",
                          "keep-window-centered"
                        ],
                        "description": "Required for resize. Choose the outcome directly. keep-left-edge-fixed means only the right edge moves. keep-right-edge-fixed means only the left edge moves. keep-window-centered means both edges move simultaneously at the same rate and the window remains horizontally centered. For a shrink where the left edge must slide right and the right edge must not move, use keep-right-edge-fixed."
                      },
                      "scale": {
                        "type": "number",
                        "minimum": 1,
                        "maximum": 4,
                        "description": "Camera magnification for a zoom action. Defaults to 1.8 when a locator is provided. Use 1 without a locator to zoom back out."
                      },
                      "state": {
                        "type": "string",
                        "enum": [
                          "attached",
                          "detached",
                          "visible",
                          "hidden"
                        ]
                      },
                      "timeoutMs": {
                        "type": "number"
                      },
                      "durationMs": {
                        "type": "number"
                      },
                      "holdAfterMs": {
                        "type": "number"
                      }
                    },
                    "required": [
                      "type"
                    ],
                    "allOf": [
                      {
                        "if": {
                          "properties": {
                            "type": {
                              "const": "resize"
                            }
                          }
                        },
                        "then": {
                          "required": [
                            "width",
                            "height",
                            "resizeMode"
                          ]
                        }
                      }
                    ]
                  }
                }
              },
              "required": [
                "name",
                "actions"
              ]
            }
          },
          "required": [
            "baseRef",
            "startCommand",
            "readyUrl",
            "scenario"
          ]
        }
      }
    ],
    "chatSkills": [
      {
        "path": "./skills/create-pr-ui-comparison/SKILL.md"
      }
    ],
    "chatInstructions": [
      {
        "path": "./instructions/pr-ui-compare.instructions.md"
      }
    ],
    "configuration": {
      "title": "PR UI Compare",
      "properties": {
        "prUiCompare.retentionDays": {
          "type": "number",
          "default": 7,
          "minimum": 1,
          "maximum": 90,
          "scope": "machine",
          "description": "Number of days to keep temporary recordings and comparison sessions outside the repository."
        },
        "prUiCompare.allowSystemBrowser": {
          "type": "boolean",
          "default": false,
          "scope": "machine",
          "description": "Allow fallback to installed Google Chrome or Microsoft Edge when managed Chromium is unavailable. Keep disabled on macOS to avoid launching applications from the system Applications folder."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test",
    "test:e2e": "npm run compile-tests && npm run compile && npm run lint && npm-run-all -s test:e2e:animation test:e2e:image",
    "test:e2e:animation": "cross-env PR_UI_COMPARE_E2E=1 vscode-test --grep \"records and renders a dirty working tree\" --timeout 180000",
    "test:e2e:image": "cross-env PR_UI_COMPARE_E2E=1 vscode-test --grep \"captures and composes final-state PNG images\" --timeout 180000",
    "check": "npm run compile && npm test",
    "package:vsix": "node scripts/package-vsix.js"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "24.x",
    "@types/vscode": "^1.125.0",
    "@vscode/test-cli": "^0.0.15",
    "@vscode/test-electron": "^3.0.0",
    "@vscode/vsce": "^3.9.2",
    "cross-env": "^10.1.0",
    "esbuild": "^0.28.1",
    "eslint": "^10.5.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.61.1"
  },
  "overrides": {
    "diff": "^8.0.4",
    "serialize-javascript": "^7.0.6"
  },
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "playwright-core": "^1.62.1"
  }
}
